@FunctionalInterface
public interface Playable {

    /*
        When we have an interface with one method
        we can describe it as having a single abstract method (SAM)
     */
    void doPlay();

}
